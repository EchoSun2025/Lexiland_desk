{
  "name": "lexiland-capture-extension",
  "private": true,
  "version": "0.2.0",
  "description": "Offline Chrome extension MVP for capturing English words into a local side panel.",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check sidepanel.js"
  }
}
